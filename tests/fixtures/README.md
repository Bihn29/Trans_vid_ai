# Test fixtures

Milestone 0 uses protocol-only deterministic fixtures. Milestone 2 uses a generated test-tool script and temporary byte media, documented under `media/README.md`. No third-party media is committed. Future fixtures must be generated in-repository or carry explicit redistribution metadata and remain short enough for offline CI.
